// Template file for pipeline configuration

// Pipeline parameters

params {

    // Pipeline inputs

    // Enter full path to samplesheet with sample IDs and paths to fastq files (incl. information if single-end or paired-end)
    // TO DO expand samplesheet to include info on strandedness and damage treatment
    samplesheet        = ''
    // Enter path to reference genome assembly
    reference          = ''

    // Enter link to decoy reference genome assembly (default: human reference genome)
    decoy              = 'https://ftp.ncbi.nlm.nih.gov/genomes/all/GCF/000/001/405/GCF_000001405.40_GRCh38.p14/GCF_000001405.40_GRCh38.p14_genomic.fna.gz'

    // Pipeline steps to be run (comma-separated list, full list: 'fastq_processing,processed_fastq_qc,mapping,mapping_concatenated_refs,raw_bam_qc,bam_processing,processed_bam_qc,random_sampling_bam')
    steps              = 'fastq_processing,processed_fastq_qc,mapping,mapping_concatenated_refs,raw_bam_qc,bam_processing,processed_bam_qc,random_sampling_bam'


    // Pipeline outputs
    // Enter path to output directory
    outdir             = 'results'

    // Mode of copying results from the work directory, values 'symlink' or 'copy'
    publish_mode       = 'copy'


    // Tool specific parameters
    // Fastp
    // Enter minimum read length for a read to be kept after adapter trimming and read merging (default: 20)
    readlength         = 20

    // Optional: Set read_len_cutoff to "auto" if you want to estimate the lower read length threshold using script <link to github>.
    read_len_cutoff = 'auto'

    // bwa-index
    // 'is' for small reference genomes such as mitogenomes, 'bwtsw' for large reference genomes
    algorithm          = 'bwtsw'

    // bwa-aln
    // Check "config/modules.config" for ancient DNA specific parameters

    // AMBER: samtools view subsample
    // Number of reads to extract for analysis in AMBER with samtools view --subsample
    // For test dataset, set to 1000. For a standard dataset, set to 1000000. For refined analysis, set to 10000000.
    subsample          = 1000000

    // Samtools depth
    // '' to exclude positions with zero depth
    // '-a' to output all positions (including those with zero depth)
    positions          = ''
    // Enter path to BED file with genome positions to include in depth calculation (e.g. repeat-mask BED file)
    intervals          = ''
    // Check "config/modules.config" for additional parameters

}

// Nextflow parameters

// Execute the script using the cached results, useful to continue executions that was stopped by an error.
resume                   = true

// If run with -profile conda or -profile mamba, provide path to centralized directory with conda environments
conda.cacheDir           = null

// If run with -profile singularity or -profile dardel, provide path to centralized directory with singularity images
singularity.cacheDir     = null

// Cluster options
params.clusterOptions   = ''

// Compute project ID for Dardel
params.project           = ''